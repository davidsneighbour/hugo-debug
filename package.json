{
  "name": "@dnb-hugo/debug",
  "version": "1.2.3",
  "license": "GPL-3.0-only",
  "description": "Hugo's missing debugging partials.",
  "author": {
    "name": "Kaushal Modi",
    "web": "https://github.com/kaushalmodi"
  },
  "maintainer": {
    "name": "Patrick Kollitsch",
    "email": "patrick@davids-neighbour.com",
    "web": "https://davids-neighbour.com"
  },
  "homepage": "https://github.com/dnb-hugo/debug#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dnb-hugo/debug.git"
  },
  "bugs": {
    "url": "https://github.com/dnb-hugo/debug/issues"
  },
  "devDependencies": {
    "@dnb-hugo/browserslist-config": "3.0.24",
    "@dnb-hugo/standard-version-config": "3.0.24",
    "@dnb-hugo/stylelint-config": "3.0.24",
    "@dnb-hugo/tools": "3.0.13"
  },
  "scripts": {
    "clean": "run-p clean:**",
    "clean:local": "rimraf package-lock.json",
    "clean:npm": "rimraf node_modules",
    "lint": "run-p lint:**",
    "lint:package": "fixpack",
    "lint:scss": "stylelint assets/scss",
    "release": "standard-version --release-as patch -a -t \"v\" && git push --follow-tags origin main",
    "release-major": "standard-version --release-as major -a -t \"v\" && git push --follow-tags origin main",
    "release-next": "standard-version --release-as minor -a -t \"v\" && git push --follow-tags origin main"
  },
  "private": true,
  "commitlint": {
    "extends": [
      "@dnb-hugo/commitlint-config"
    ]
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-customizable"
    }
  }
}
